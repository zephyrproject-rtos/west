# west — Phase 1 python compat layer
#
# This package will be populated from src/west/ (the existing
# pure-python implementation, minus deprecated members) in a
# follow-up commit. For now it carries the minimum surface the
# rust extension dispatch path needs: WestCommand, exceptions,
# and the _dispatch bridge.
